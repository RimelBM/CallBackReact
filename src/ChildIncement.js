import React from 'react'

function ChildIncrement({call}) {
    return (
        <>

            <button onClick={call}  > + </button>
            
        </>
    )
}

export default ChildIncrement
